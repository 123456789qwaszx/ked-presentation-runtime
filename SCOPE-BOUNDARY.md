# 저장소 경계 — 이 런타임이 하는 일과, 여기서 내보낸 일

## 0. 한 문장

> **이 저장소는 yarn 노드 하나를 재생한다. 저작·진행·저장은 밖이다.**

---

## 1. ⚠ 이 문서를 잘못 읽는 법

아래 §3에 "여기서 빠졌다"고 적힌 것들은 **전부 반드시 필요한 기능이다.**
없어도 되는 것이어서 지운 게 아니다. **주인을 옮기는 중이라서 이 저장소에서만 임시로 뺐다.**

"필요 없어서 지웠다"로 읽으면 이 문서는 실패한 것이다. 각 항목에 **새 주인**과
**돌아오는 모양**을 같이 적어 둔 이유가 그것이다.

이것은 규율 1(침묵 금지)의 문서판이다 — 코어가 못 접은 커맨드를 `Unhandled`에 남기듯,
저장소가 내보낸 기능도 목록으로 남긴다. **빠진 것을 조용히 두지 않는다.**

---

## 2. 왜 이 경계인가

저작 위치가 바뀌었다.

```
예전:  Unity 안에서 yarn을 쓴다 → 쓴 것이 곧 연출이다
지금:  VnTool에서 저작한다 → .yarn 텍스트가 산출된다 → 이 런타임이 재생한다
```

저작이 밖으로 나가면 **여러 축의 주인이 따라 바뀐다.** 그런데 옮기지 않고 두면 같은 것이
양쪽에 살아남는다. 그때 적용한 원칙 하나:

> **두 곳에 있으면 갈린다. 더 나은 한쪽만 남긴다.**

`Ked.Presentation.Core` 추출이 이미 같은 원칙이었다 — 정지 프레임 계산이 툴과 런타임에
각각 있으면 갈리므로, 순수 층 하나를 양쪽이 공유한다.

---

## 3. 밖으로 나간 것

### 3.1 서브 레인 — Story + Set + Pres 트리오

| | |
|---|---|
| **무엇** | 대사 레인과 별도로 도는 연출 전용 레인. `pres_start` / `pres_end` / `pres_hold` / `pres_advance`, 락스텝 게이트, 연출 전용 라인(`#beat`) |
| **왜 있었나** | **저작이 Unity 안에 있었다.** yarn에 직접 쓴 것이 곧 연출이었으므로, "대사 줄"과 "연출 줄"의 분리를 런타임이 **실행 시점에** 해야 했다 |
| **왜 여기서 빠졌나** | 저작이 VnTool로 나갔다. **테이블 분리는 이미터의 일이다** — 런타임이 커서 두 개를 돌려 락스텝을 맞추는 것은 같은 문제를 두 번 푸는 것이다.<br>그리고 그 비용이 컸다: 레인이 둘이면 "누가 전진을 소비했는가"가 **항상 두 갈래**가 되어, 디버깅과 신규 학습 비용이 자릿수로 오른다 |
| **새 주인** | **VnTool 이미터.** 어느 커맨드가 어느 라인에 붙는지를 텍스트로 확정해 내보낸다. 런타임은 파싱과 실행만 한다 |
| **여기 남은 대체** | 시간 축 분리는 **수명 스코프**가 표현한다 — `IStepScopedCommand`(다음 라인 경계에서 정리) / `IRunScopedCommand`(세션 끝까지 산다). 커서를 늘리지 않고 같은 표현력을 얻는다 |
| **보존** | 게이트 기계 자체는 지우지 않았다. `Assets/Ked.Presentation.Sync/`에 **엔진 의존 0으로 순수화해 보관**돼 있다. 두 흐름을 프레임 단위로 동기화하는 문제가 다시 생기면 여기서 꺼낸다 |
| **커밋** | `a220a5a8` (subLane 제거) · `8eeb2829` (InlineAdvance·SyncHub 제거) · `4cd185a8` (Sync 순수화 보관) · `a2f7e7af` (연출 전용 라인 제거) |

### 3.2 세이브 · 로드

| | |
|---|---|
| **무엇** | `{nodeName, lineId}` 저장 + 노드 처음부터 리플레이 시킹. 선택 기록은 위치 기반 |
| **왜 여기서 빠졌나** | 저장 **위치**가 아니라 저장 **대상**이 틀렸다.<br>**경로를 저장하는 세이브는 대본이 불변일 때만 성립한다.** 이 프로젝트의 전제는 정반대다 — 작가가 툴에서 계속 고치고 계속 재출력한다 |
| **그 전제에서 나온 ⚠들** | `#line:` 태그가 없으면 로드가 타깃을 못 찾아 **silent hang** (계약서 C1)<br>출시 후 선택지 순서가 바뀌면 **기존 세이브가 다른 선택지를 리플레이** (C3)<br>변수 스냅샷이 없어 `<<set>>`이 **결정적이어야만** 함 (C5)<br>→ 셋 다 리플레이 방식의 파생이다. 저장소를 바꿔서는 해결되지 않는다 |
| **돌아오는 모양** | **스냅샷 기반으로.**<br>`세이브 = { nodeName, lineId, 변수, StageState }`<br>`로드 = StageState 적용 → 그 라인부터 재생` (리플레이 없음)<br>→ 위 ⚠ 셋이 **원천 소멸**한다 |
| **재료는 이미 있다** | `Ked.Presentation.Core`의 `StageState` / `StageReducer`가 정확히 *"N번 라인 시점의 확정된 무대"*다. 등가성 하네스에 이은 **정지 프레임의 두 번째 고객**이 세이브다 |
| **저장 위치** | 런타임이 정하지 않는다. `ISaveStore` 같은 포트를 두고 구현은 호스트가 갖는다 — `IInputSource` · `ITimeSource` · `ILaneRunner`와 같은 규율 |
| **커밋** | `18df6f78` |

### 3.3 에피소드 · 챕터 진행

| | |
|---|---|
| **무엇** | 에피소드 정의 SO, 챕터 진행 규칙, 언락·전이 판정, 진행 JSON 수입기 |
| **왜 여기서 빠졌나** | 같은 것이 **양쪽에 있었다.** VnTool 쪽이 훨씬 앞서 있다 — 챕터 그래프 편집, 도달성 증명, `progression.json` 내보내기까지 실동작한다.<br>두 구현이 있으면 갈린다. 계약서 G7이 그 위험을 이미 적어 두었다 — *툴의 도달성 증명은 min/max로 clamp하며 걷는데 런타임은 경계를 모른다* |
| **새 주인** | **별도 프로젝트(순수 C#).** 에피소드·챕터를 잇고 그리는 방식은 **게임 공용**이다. 순수 층으로 빼면 툴과 런타임이 **같은 평가기**를 쓰므로 G7의 불일치가 원천 소멸한다.<br>`Ked.Presentation.Core`가 그 선례다 — 같은 수를 진행 층에 한 번 더 두는 것이다 |
| **이 저장소와의 경계** | `EpisodePlayer.StartGameAsync(nodeName)`.<br>진행 층은 **"이 노드를 재생해 줘"** 까지 말하고, 그 안쪽은 이 저장소가 전부 책임진다 |
| **커밋** | `1805c555` |

### 3.4 플레이어 스탯 — 카탈로그만

| | |
|---|---|
| **뺀 것** | 스탯의 **의미** — 어떤 스탯이 있는지, 초기값·최소·최대, 어떤 언락에 쓰이는지 |
| **안 뺀 것** | 변수 저장소와 조건 평가 — 이름→숫자, `>=` · `<` · `==` 판정. **게임 무관이므로 공용이다** (현재는 Yarn의 `<<set>>` / `<<if>>`가 담당) |
| **왜** | 공용 기반에 특정 게임의 어휘가 들어가면 그 순간 공용이 아니다. 규율 3의 확장 — 게임 데이터는 코드가 아니라 `game.definition.json`이 공급한다 |
| **⚠ 주의** | 뺀 것은 "스탯"이 아니라 **"스탯 카탈로그"**다. 조건 평가기까지 없다고 읽으면 진행 층을 붙일 때 막힌다 |

---

## 4. 여기 남는 것 — 이 저장소의 책임

| 책임 | 위치 |
|---|---|
| yarn 텍스트 → 커맨드 스펙 파싱 | `Scripts/YarnBridge/` |
| 커맨드 실행과 수명 (91종) | `Scripts/Ked.Presentation.Runtime/` |
| 한 라인의 표시 트랜잭션 — 진입·시크·박스·타자기·전진 대기 | `Scripts/DialoguePresenter/VNLinePresentationFlow/` |
| 전진 판정 · 롤백 시킹 · 백로그 | `Scripts/FeatureController/` |
| 정지 프레임 계산과 등가성 판정 | `Ked.Presentation.Core/` + `Game/StageEquivalenceHarness.cs` |

한 줄로: **노드 하나가 들어오면, 그것을 화면에 정확히 재생한다.**

---

## 5. 함께 걷혔으나 아직 집이 없는 규율 하나

**`Ready` / `Released`** — "취소로 무너진 라인은 완료로 세지 않는다."

이것은 **레인의 성질이 아니라 라인의 성질**이다. 레인이 하나든 둘이든 성립한다.
그런데 구현이 서브 레인 안에만 있었고, 레인을 걷을 때 함께 걷혔다.

`MarkLineTornDown`으로 자리는 옮겨 두었으나(`VNLinePresentationState`), 현재는
`IsLineFullyShown`을 끄기만 하고 **전진 소모를 막지는 않는다.**

> 2026-08-17에 잡은 롤백 버그가 이 공백의 재발이었다. 같은 부류를 세 번째 잡은 것이다
> (`e38f16f4` 서브 레인 · `652af073` 원샷 러너 · `9e70a487` 메인 레인).

→ **단일 레인용 구현이 필요하다. 레인을 되살리는 일이 아니다.**

---

## 6. 계약서(`runtime-contract.md`)와의 관계

VnTool ↔ 런타임 계약서는 §3의 이동을 아직 반영하지 않았다. 개정 전까지 아래를 기준으로 읽는다.

| 절 | 상태 | 사유 |
|---|---|---|
| A (트리오·서브 레인 수명) | **폐기** | §3.1 — 런타임은 단일 레인이다. Pres/Set 노드를 내지 않는다 |
| B (라인 동기화 예산) | **폐기** | §3.1 — 레인이 하나이므로 예산 개념이 없다 |
| C (식별자·세이브 호환) | **보류** | §3.2 — 세이브가 없다. 재도입 시 스냅샷 기반이며 `#line:` 태그에 의존하지 않는다 |
| D (변수·조건·선택지) | **유효** | 단 D1(세 러너 변수 저장소 공유)은 러너가 하나이므로 무의미 |
| E1·E3·E4 (커맨드 어휘·기본값·매크로) | **유효 — 계약의 본체** | 이 저장소가 지켜야 하는 실질이 여기로 좁혀졌다 |
| E2 (메인레인 전용 커맨드 금지) | **폐기** | §3.1 — 서브 러너가 없다 |
| E5 (`#beat` 등 라인 메타태그) | **부분 폐기** | 비트 계열은 제거(`a2f7e7af`). 박스 종류·전환 태그는 유효 |
| G (챕터 진행 JSON 수입기) | **이 계약서 밖** | §3.3 — 진행 층은 다른 저장소의 계약이다 |

**계약이 "커맨드 어휘"로 줄어든 것이 §3의 성과다.** 검증 가능한 크기가 되었으므로,
툴 출력 .yarn을 픽스처로 고정하는 골든 테스트가 현실적인 다음 단계다.

---

## 7. 미완 정리

빠진 것을 조용히 두지 않는다는 규율을 이 목록에도 적용한다.

### 열린 것

| | 내용 |
|---|---|
| 보관 어셈블리 | `Ked.Presentation.Sync` — 의도된 보관(§3.1)이나 **소비자와 테스트가 모두 0**이다. 실행되지 않는 코드는 검증되지 않는다. 붙이거나 · 테스트를 달거나 · 트리에서 빼거나 중 하나를 골라야 한다 |
| 생산자 없는 값 | `VNSeekKind.Load` — 이 값을 만드는 코드가 없다(`BeginLoadSeek` 제거와 함께). **§3.2의 스냅샷 세이브가 돌아올 자리라 남겨 두었다.** 그때까지 시크 종류는 `Rollback` 하나뿐이다 |
| 시크 종류의 미사용 | `SeekKind`는 `VNSeekLineDecision`으로 실려 다니지만 **분기하는 코드가 없다.** 현재는 진단용이다 |

### 닫힌 것

| | |
|---|---|
| 씬 잔해 | 정리 완료 — `SampleScene` · `DungeonCafe` 삭제, `PresentationSample.unity` 하나만 남았다. `OneShot*` · `subPresentationRunner` 참조 0건, 러너는 하나다 |
| 에디터 도구 | `VNRuntimeStateInspectorWindow`의 `[VNLoadSeekDriver]` 절 제거. 함께 죽어 있던 `_presentationSessionContext` 리플렉션 대상을 `_presentationScopeSession`으로 교정 |
| 죽은 진입점 | `VNLinePresentationState.BeginLoadSeek` 제거 |

> ⚠ `VNRuntimeStateInspectorWindow`는 **필드 이름 문자열로 리플렉션**한다. 필드가 사라지면
> 오류가 아니라 `null`이 찍히므로, 이 창의 항목은 리팩터 때 조용히 죽는다. 위 두 건이 그 예다.
> 새 항목을 넣을 때는 이 성질을 감안할 것.
